<div id='tabs'>
    <div id='logo'><span style="margin-right: 2px; color: var(--primary-color)"><span style='margin-right: 15px'>></span>paper</span>trail</div>
    <div class='container' style='position: absolute;left: calc(50% - 75px);'>
        <h3 id='users-tab' class='tab' onclick='goToUsers()'>Users</h3>
        <h3 id='events-tab' class='tab' onclick='goToEvents()'>Events</h3>
    </div>
    <div class='container'>
        <div id='dashboard-tab' class='tab' onclick='goToDashboard()'>Dashboard</div>
        <div id='settings-tab' onclick='goToSettings()'></div>
    </div>
</div>

<script>
    let page = 'users';

    if(window.location.pathname == '/user'){
        page = 'user'
    }
    if(window.location.pathname == '/events'){
        page = 'events'
    }
    if(window.location.pathname == '/dashboard'){
        page = 'dashboard'
    }
    



    if(page == 'users' || page == 'events' || page == 'dashboard'){
        document.getElementById(page + '-tab').classList.add('selected')
        document.getElementById(page + '-tab').onclick = ""
    }

    function goToUsers(){
        document.location.href = '/users'
    }
    
    function goToEvents(){
        document.location.href = '/events'
    }

    function goToSettings(){

    }

    function goToDashboard(){
        document.location.href = '/dashboard'
    }
</script>

<style>
    :root{
        --navbar-inset: 60px;
    }
    #tabs{
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        align-items: center;

        height: 90px;
        width: calc(100% - 60px);
        padding: 0;
        margin: 0px auto 50px auto;

        border-bottom: 1px solid #eee;
    }
    .tab{
        font-family: Lato!important;
        height: fit-content;
        width: fit-content;
        font-family: Open Sans;
        user-select: none;
        cursor: pointer;
        border-radius: 2px;
        padding: 4px 8px 5px 8px;
        font-size: 17px;
        background: transparent;
        color: var(--primary-gray);
        margin: auto 6px;
    }
    .tab:hover{
        background-color: rgba(0,0,0,.05);
    }
    .selected{
        background-color: var(--primary-gray)!important;
        color: white;
    }
    #settings-tab{
        width: 30px;
        height: 30px;
        background: url("/settings_cog.png");
        background-size: contain!important;
        opacity: .75;
        cursor: not-allowed;
        margin-left: 20px;
    }
    #projects-tab{
        user-select: none;
        cursor: pointer;
        font-size: 15px;
    }
    #logo{
        font-size: 30px;
        text-indent: 20px;
        user-select: none;
        font-family: Open Sans!important;
        color: var(--primary-gray-inverse);
    }
    .container{
        display: flex;
        flex-direction: row;
        justify-content: center;
        align-items: center;
    }
</style>